---
description: 所有实验（含测试、内部试跑）默认申请一块 4090
alwaysApply: true
---

# 默认用一块 4090 跑实验

除非用户明确指定别的卡、分区或只要 CPU，否则本仓库里所有需要执行的实验都必须先申请 **一块 RTX 4090**，再跑命令。

覆盖范围包括：正式训练、评测、单元/集成测试、内部冒烟或冒泡试跑、`--demo` 小规模验证。不要在登录节点直接跑这些任务。

申请交互式 4090：

```bash
srun -A duanty --gres=gpu:1 --pty bash
conda activate moe
```

申请到 GPU 后再执行 `python main.py ...` 或测试命令。用户指定 H800、CPU 或其他分区时，才改用对应资源。
